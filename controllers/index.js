const {listContacts, getById, removeContact, addContact, updateContact, updateFavorite} = require('./contacts');

module.exports = {
    listContacts,
    getById,
    removeContact,
    addContact,
    updateContact,
    updateFavorite
}
